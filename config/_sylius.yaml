sylius_grid:
  grids:
    app_admin_catalog:
      driver:
        name: doctrine/orm
        options:
          class: App\Entity\Catalog\Catalog
      fields:
        name:
          type: string
          label: sylius.ui.name


